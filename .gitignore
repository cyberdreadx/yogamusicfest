# OS / editor cruft
.DS_Store
Thumbs.db
*.log
.vscode/
.idea/

# Netlify local
.netlify/

# Node / Stripe function deps
node_modules/
npm-debug.log*
package-lock.json

# Secrets — NEVER commit your Stripe keys (set them as Netlify env vars)
.env
.env.*
